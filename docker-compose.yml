version: '3.4'

services:
  texto.api:
    image: malekatwiz/texto.api
    build:
      context: .
      dockerfile: Texto.Api/Dockerfile
    ports:
      - "80"
    environment:
      - TwilioSettings__Sid
      - TwilioSettings__Token
      - AzureBus__ConnectionString
      - AzureBus__QueueName
      - Authority